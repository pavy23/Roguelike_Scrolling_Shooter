# ART-DIRECTION.md — HD 도트(hi-bit) 기준 v2

사람(아트 디렉터) + RENDERER가 관리한다. 에이전트는 이 규격을 벗어나는 아트를 임포트하지 않는다.
v1(384×224 파일럿)은 2026-07-28 캔버스 상향 결정으로 대체됨 — ROADMAP.md 참고.

## 방향

- **hi-bit 픽셀아트**: 캔버스 **640×360 (16:9)**, PPU 16. SNES 감성의 실루엣·가독성에 현대 수준의 팔레트·셰이딩·애니메이션 밀도. 8비트 회귀 금지.
- **레퍼런스**: 그라디우스 V, R-Type(생체+기계 융합, 거대 보스 실루엣), Blasphemous·Huntdown(640×360급 hi-bit 셰이딩 밀도).
- 톤: 어두운 우주 배경 위에 채도 높은 기체/탄/이펙트. 탄과 아이템은 항상 최고 가시성 — 배경보다 2단계 이상 밝게.

## 에셋 규격 (v2)

| 에셋 | 크기(px) | 파일 | 애니메이션 |
|---|---|---|---|
| 플레이어 기체 | 48×30 | player_ship.png / .ase | 아이들, 상/하 뱅킹, 엔진 화염 (4~6프레임) |
| 잡졸 (zako) | 24×24 | enemy_<id> | 아이들 2~4프레임 + 사망 |
| 중형/터렛 | 32×32 ~ 48×48 | enemy_<id> | 공격 예열 + 사망 |
| 미니보스 | 96×96급 | miniboss_<id> | 페이즈별 상태 |
| 스테이지 보스 | 128×128 ~ 256×160 | boss_<id> | 멀티파트, 단계별 파괴 |
| 거대 전함 함체 (St3) | **320×160** | warship_hull.png | 정적 1장 (파츠는 별도 스프라이트가 위에 얹힌다) |
| 파워업 캡슐 | 16×14 | capsule.png | 발광 펄스 2~4프레임 |
| HUD 슬롯 아이콘 | 24×12 | hud_icon_<slot> | 정적 |
| 폭발 (소/중/대) | 24 / 48 / 96 | fx_explosion_<size> | 6~10프레임 시트 |

- 투명 배경(알파), **팔레트 에셋당 48색 이내**, 안티앨리어싱 없는 하드 픽셀 경계.
- 배경: 테마당 패럴랙스 3~4겹 (원경 성층 / 중경 구조물 / 근경 실루엣 / 전경 파티클), 겹당 640×360 이상 타일러블.
- 임포트 설정은 BattleSceneBuilder/임포트 스크립트가 일괄 처리 (프로젝트 표준 Retro AA).
- ~~비어 있는 아트 슬롯~~ **채워짐 (2026-08-03)**: `art-input/warship_hull.png` (St3 거대
  전함 함체, 320×160). gpt-image-1.5 생성 2안 중 A안 채택(사람 지시로 제작) —
  원본·대안 B안·프롬프트는 `Tools/ArtGen/out/`(raw/final)에 보존. 교체하려면 같은
  파일명으로 덮고 씬 재생성만 하면 된다.
  도안 요구(유지): 뱃머리 왼쪽(진행 방향), 함미 오른쪽. 상/하 갑판에 포탑 하드포인트
  4곳(로컬 오프셋 ±3.5·±2 유닛)이 얹히므로 그 자리는 평평한 장착면으로 비워 둘 것.

## 생성 파이프라인 (2026-07-28 파일럿 결과로 개정)

| 용도 | 도구 | 비고 |
|---|---|---|
| 스프라이트 전반(≤128px: 기체·적·이펙트·보스 파츠)·애니 프레임·회전 | **PixelLab API** (api.pixellab.ai/v2) | **파일럿 승자.** pixen 네이티브 저해상도 생성(크기는 4의 배수) → post로 규격 정리. animate-with-text-v3(비동기 job)로 프레임 일관성 확보 |
| 대형 키프레임(보스 160px+, 배경 원화 640×360) | **OpenAI gpt-image-1.5** | `background: transparent` 지원. **gpt-image-2는 투명 배경 미지원**이라 사용 불가. 배경 원화는 `--opaque`가 필수(투명을 강제하면 하늘·바닥을 비운다). 소형으로 다운스케일하면 뭉개진다 — 아래 재확인 참고 |
| SFX | **Tools/SfxGen** (자체 sfxr 스타일 절차 합성, 무료·결정론) | (타입, 시드)가 원본. 부족한 질감은 CC0 팩(Kenney 등)으로 보완. 유료 AI 오디오는 보스 특수음 등 필요 시 단발 검토 |

### 2026-08-04 재확인 — 소형 스프라이트는 반드시 PixelLab

사람 요청으로 같은 프롬프트를 두 모델에 돌려 64×48 결과를 나란히 봤다
(`Tools/ArtGen/out/bakeoff/compare_b.png`).

- **gpt-image-1.5 → 다운스케일**: 실루엣이 사라지고 회색 덩어리가 됐다. 디더가
  픽셀 격자를 덮어 무엇인지 읽히지 않는다.
- **PixelLab 네이티브**: 외곽선이 살아 있고 포신·손상부가 한눈에 읽힌다.

이 차이는 취향이 아니다. gpt-image는 "픽셀아트 **풍**" 고해상 그림을 그리고,
그것을 64px로 줄이면 원래 없던 격자를 만들어 내야 해서 뭉갠다. PixelLab은
처음부터 그 격자로 그린다.

**그런데 이 세션에서 전함 코어·잔해를 gpt-image로 만들었다.** 80×80과 64×48짜리를
대형 키프레임 도구로 만든 것이다 — 위 표를 읽고도 그랬다. PixelLab으로 다시
뽑으니 확연히 나았다. 크기를 먼저 보고 도구를 고를 것.

### 같은 날 4종 비교 — 결론

사람이 키를 주어 Grok까지 돌렸다 (`Tools/ArtGen/out/bakeoff/`).

**소형 스프라이트(64×48 파괴된 포탑)** — `compare_3.png`

| 모델 | 결과 |
|---|---|
| **PixelLab** | **승.** 외곽선이 살고 포신이 날아간 것이 한눈에 읽힌다 |
| gpt-image-1.5 | 회색 덩어리. 형태가 사라졌다 |
| Grok | 회색 덩어리. 색조차 남지 않았다 |

두 범용 모델이 같은 이유로 진다 — 고해상 "픽셀아트 **풍**"을 그린 뒤 64px로
줄이면 없던 격자를 만들어 내야 해서 뭉갠다. PixelLab은 처음부터 그 격자로 그린다.
**소형은 논쟁의 여지가 없다.**

**배경 원경(640×360)** — `compare_bg.png`

PixelLab은 입력·출력 256px 상한이라 애초에 불가. gpt-image vs Grok은 접전이었다.
Grok 쪽이 분위기와 깊이감이 낫지만 **그라데이션이 부드러워 도트가 아니다** —
위에 얹히는 픽셀 스프라이트와 화풍이 어긋난다. 이 프로젝트의 정체성이 SNES
도트이므로 gpt-image를 유지한다. (회화풍 배경이 필요해지면 Grok을 다시 본다.)

**그리고 Grok은 투명 배경과 크기 지정을 지원하지 않는다** — 전경·중경·랜드마크는
투명이 필수라 애초에 후보가 아니다.

**Nano Banana(Gemini)** — 사람이 결제를 켜서 마저 돌렸다 (`compare_4.png`).

- **생성**은 gpt-image·Grok과 같은 실패다. 회색 덩어리에 포신은 멀쩡했다.
- **편집(`--ref`)은 다르다.** 원본 포탑의 팔레트와 블록감을 거의 그대로 물려받았다 —
  네 장을 나란히 놓으면 이것만 "같은 게임의 그림"으로 보인다. 내가 기대했던
  강점이 실제로 있었다.
- 다만 **내용이 못 미친다.** "부서진 포탑"이 아니라 조각 두 개로 흩어졌다.

그래서 채택하지 않았지만 버리지도 않는다. **기존 스프라이트의 팔레트를 정확히
물려받아야 하는 변형**(손상 단계, 색만 다른 아종)에는 이쪽이 유일한 수단이다.
한 번씩만 돌린 비교라는 점은 분명히 해 둔다 — 프롬프트를 다듬으면 결과가 달라질
여지가 있다.

### 확정 규칙

| 대상 | 도구 |
|---|---|
| ≤128px 스프라이트·보스 파츠·애니 프레임 | **PixelLab** |
| 기존 스프라이트의 팔레트를 물려받는 변형 | **Gemini `--ref`** (내용은 검수 필수) |
| 투명이 필요한 레이어(fg·mid·landmark) | **gpt-image-1.5** |
| 불투명 원경 배경 640×360 | **gpt-image-1.5** (`--opaque`) |

크기를 먼저 보고 도구를 고를 것. 이 세션에서 80×80 코어와 64×48 잔해를
gpt-image로 만들었다가 다시 뽑았다.

절차:
1. `Tools/ArtGen/` 스크립트로 생성 (프롬프트·파라미터·시드를 산출물과 함께 커밋)
2. 후처리: 다운스케일(최근접) → 팔레트 양자화 → 규격 검증
3. Unity CLI `import_asset`으로 임포트 → `capture_game_view`로 인게임 확인 캡처
4. 사람 큐레이션: 합격작만 `Assets/Art/`에 확정. 애니메이션은 Aseprite(.ase) 경유로 클립 자동 생성

### 프롬프트 템플릿 (gpt-image-2)

```
Pixel art sprite of a sleek sci-fi space fighter ship, side view facing right,
hi-bit 16-bit style like SNES-era Gradius and R-Type with modern shading,
3-4 tone ramps per material, dark blue and steel hull with orange engine glow,
hard pixel edges, no anti-aliasing, transparent background, 192x120 canvas,
limited 48 color palette
```

적/보스는 대상만 교체: `...biomechanical alien drone enemy, R-Type inspired
organic-mechanical fusion, menacing silhouette readable at 24x24...`

## 비고

- 좌표/히트박스는 GameData JSON이 원본 — 아트 크기 변경 시 halfWidth/halfHeight는 GROK을 통해 갱신 (REQ-006 참고).
- 코드 생성 플레이스홀더(BattleSceneBuilder 픽셀 배열)는 해당 에셋이 실파일로 교체되는 시점에 제거.
